<h1>Listing Profiles</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @profiles.each do |profile| %>
      <tr>
        <td><%= link_to 'Show', user_profiles_path %></td>
        <td><%= link_to 'Show', user_profiles_path %></td>
        <td><%= link_to 'Edit', edit_user_profile_path(profile.id, profile.user_id) %></td> 
        <td><%= link_to 'Destroy', user_profile_path(profile.id, profile.user_id), method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>


<%= link_to 'New Profile', new_user_profile_path %>

